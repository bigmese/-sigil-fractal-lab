# Architecture

`index.html` is the interface, `app.js` is the complete browser runtime, and `database/` is the released Atlas snapshot. The browser reads only released files. Raw evidence remains separate from experimental generation logic.
