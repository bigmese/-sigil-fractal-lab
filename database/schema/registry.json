{
  "dataset_version": "0.1.0",
  "primary_keys": {
    "sources": "source_id",
    "observations": "observation_id",
    "lineages": "lineage_id",
    "geometry_families": "geometry_id",
    "meanings": "meaning_id",
    "claims": "claim_id",
    "hypotheses": "hypothesis_id",
    "dna_candidates": "candidate_id",
    "rescreen_queue": "queue_id",
    "checkpoints": "checkpoint_id",
    "integrity": "check_id",
    "recovery_staging": "staging_id",
    "export_corrections": "correction_id"
  },
  "foreign_keys": {
    "observations.source_id": "sources.source_id",
    "claims.source_id": "sources.source_id",
    "claims.observation_ids[]": "observations.observation_id",
    "meanings.source_claim_ids[]": "claims.claim_id",
    "meanings.observation_ids[]": "observations.observation_id",
    "meanings.geometry_ids[]": "geometry_families.geometry_id",
    "geometry_families.lineage_ids[]": "lineages.lineage_id",
    "geometry_families.meaning_ids[]": "meanings.meaning_id",
    "geometry_families.representative_observation_ids[]": "observations.observation_id",
    "lineages.first_source_id": "sources.source_id"
  },
  "field_policy": {
    "null": "unknown or not recorded",
    "empty_array": "no linked records",
    "lineage_weight": "weight used to limit duplicate or inherited-lineage inflation",
    "inspection": "distinguishes visual inspection from text-only or metadata evidence",
    "authoritative_status": "records whether the row belongs to the trusted active corpus"
  }
}
